<div class='speakers'>
  <h1 class='title'>Speakers</h1>

  <div class='speakers-list'>
    {{#each model as |speaker|}}
      <div class='speaker'>
        {{#link-to 'speakers.speaker' speaker}}
          <img {{bind-attr src=speaker.imageUrl}} class='speaker-image'>
        {{/link-to}}
      </div>
    {{/each}}
  </div>

  <div class='speakers-speaker-content'>
    {{outlet}}
  </div>
</div>
